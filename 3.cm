/* PROGRAM #3
   This program generates a syntax error by not including
   a semicolon in a variable declaration on line 18.
*/

int func2(int a) {
    return a * 4;
}

void func1(int x, bool y) {
    output(x + y);
    output(func2(x + y));
}

int main(void) {
    int x;
    int y;
    int z

    x = (y - z) / x;
    func1(x, z);

    return x;
}
