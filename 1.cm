/* PROGRAM #1
   This program compiles correctly.
*/

int x;
int y;

int func1(int a, int b) {
    return (a + b) / 4;
}

int main(void) {
    int x;
    int y;

    x = 8;
    y = 12;

    {
        int x;
        int y;
        x = 1;
        y = 2;
        output(x + y);
    }

    return func1(x, y);
}
