/* PROGRAM #1
   This program compiles correctly.

   Tests variable declarations, assignments, operator expressions,
   and function calls.

   The output is 25.
*/

int x;
int y;

int func1(int a, int b) {
    return (a + b) / 4;
}

void main(void) {
    int x;
    int y;

    x = 8;
    y = 12;

    output(x + y + func1(x, y));
}
